create table timeseries_test
(
    timestamp timestamp,
    length numeric,
    symbol text,
    open numeric,
    close numeric,
    high numeric,
    low numeric,
    volume bigint,
    CVD bigint
);